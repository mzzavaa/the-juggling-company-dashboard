version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd webapp
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: webapp/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
